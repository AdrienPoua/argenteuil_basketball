/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['picsum.photos', "i.ibb.co", "images.unsplash.com", "placehold.co", " via.placeholder.com"],
  }
};

export default nextConfig;

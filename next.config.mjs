/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['picsum.photos', "i.ibb.co", "images.unsplash.com", "placehold.co", " via.placeholder.com", "www.argenteuil.fr"],
  }
};

export default nextConfig;
